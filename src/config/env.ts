// Environment configuration
// For production, use environment variables or a secure method to store API keys

export const ENV = {
  // Add your API keys here as needed
  // Example: API_KEY: process.env.VITE_API_KEY || "fallback_key"
};

export default ENV;
